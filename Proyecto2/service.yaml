apiVersion: v1
kind: Service
metadata:
  name: ingenieria-service
spec:
  type: NodePort
  selector:
    app: ingenieria-app
  ports:
  - port: 8000
    targetPort: 8000

---
apiVersion: v1
kind: Service
metadata:
  name: agronomia-service
spec:
  type: NodePort
  selector:
    app: agronomia-app
  ports:
  - port: 8000
    targetPort: 8000